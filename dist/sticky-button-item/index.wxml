<button class="m-sticky-button" style="{{ bgcolor ? 'background-color:' + bgcolor + ';' : '' }} {{ toggle ? style : '' }}">
  <m-icon custom-class="custom-icon" name="{{ icon }}" size="{{ iconSize }}" color="{{ iconColor ? iconColor : '#fff' }}" />
</button>