.m-sticky-button {
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: auto;
  padding: 0;
  width: 100%;
  height: 100%;
  left: 0;
  top: 0;
  color: #ffff;
  background: var(--color-primary, #409EFF);
  border-radius: 50%;
  position: absolute;
  z-index: 1;
  border: 0 !important;
  transition: transform .3s ease-in-out;
}

.m-sticky-button::after {
  display: none;
}

.custom-icon {
  vertical-align: middle !important;
}