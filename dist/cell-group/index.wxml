<view class="custom-class m-cell__group {{ card ? 'm-cell__group--card':'' }}">

	<view wx:if="{{ title }}" class="m-cell__group__header">
    <view class="m-cell__group__title">{{ title }}</view>
    <slot name="group-suffix"></slot>
  </view>

  <view class="m-cell__list">
    <slot></slot>
  </view>
</view>