<view 
  class="custom-class m-message m-message--{{type}} {{ visible ? '' : 'is-hidden' }}"
  style="{{ 'top:' + top  }};{{ 'z-index:' + zIndex }}" 
>
  {{ content }}
  <view class="m-message__close-btn" wx:if="{{showClose}}">
    <view class="btn" bindtap="onClose"></view>
  </view>
</view>